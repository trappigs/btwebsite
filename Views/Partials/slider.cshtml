<style>
/* Slider Button Styles */
.slide-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background: linear-gradient(transparent, rgba(0,0,0,0.8));
    color: white;
    padding: 30px 20px 20px;
    text-align: center;
}

.slide-overlay h4 {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 5px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

.slide-overlay p {
    font-size: 0.9rem;
    margin-bottom: 15px;
    opacity: 0.9;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
}

/* Hover Buttons */
.slide-hover-buttons {
    display: flex;
    flex-direction: column;
    gap: 8px;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.3s ease;
}

.slide-item:hover .slide-hover-buttons {
    opacity: 1;
    transform: translateY(0);
}

.slide-btn {
    padding: 6px 16px;
    text-decoration: none;
    border-radius: 15px;
    font-weight: 600;
    font-size: 0.75rem;
    transition: all 0.3s ease;
    text-align: center;
    display: block;
    border: 2px solid;
    background: transparent;
    cursor: pointer;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.slide-btn.btn-project {
    background: #045129;
    color: white;
    border-color: #045129;
}

.slide-btn.btn-project:hover {
    background: #067833;
    border-color: #067833;
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(4, 81, 41, 0.4);
    color: white;
    text-decoration: none;
}

.slide-btn.btn-form {
    color: white;
    border-color: white;
}

.slide-btn.btn-form:hover {
    background: white;
    color: #045129;
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(255, 255, 255, 0.3);
}

/* Responsive */
@@media (max-width: 768px) {
    .slide-overlay {
        padding: 25px 15px 15px;
    }
    
    .slide-overlay h4 {
        font-size: 1.1rem;
    }
    
    .slide-overlay p {
        font-size: 0.85rem;
        margin-bottom: 12px;
    }
    
    .slide-btn {
        padding: 5px 12px;
        font-size: 0.7rem;
    }
    
    .slide-hover-buttons {
        gap: 6px;
    }
}

@@media (max-width: 480px) {
    .slide-overlay {
        padding: 20px 12px 12px;
    }
    
    .slide-overlay h4 {
        font-size: 1rem;
    }
    
    .slide-overlay p {
        font-size: 0.8rem;
        margin-bottom: 10px;
    }
    
    .slide-btn {
        padding: 4px 10px;
        font-size: 0.65rem;
    }
}
</style>